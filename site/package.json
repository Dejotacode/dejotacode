{
  "name": "@dejotacode/site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "npm run build && wrangler pages deploy dist --project-name dejota-code --branch main"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.0",
    "wrangler": "4.130.0"
  }
}
