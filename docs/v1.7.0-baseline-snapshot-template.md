# Template de snapshot quantitativo — v1.7.0

## Uso

Preencher somente nas janelas oficiais D+7 e D+14 definidas em `v1.7.0-measurement-protocol.md`.

Este documento é um formulário de evidência. Não autoriza mudança de CTA, campanha, formulário, trilha ou analytics.

## Identificação da extração

- checkpoint: `D+7` ou `D+14`;
- extraído em UTC: `YYYY-MM-DDTHH:MM:SSZ`;
- janela UTC: `2026-09-16 <= metric_date < LIMITE`;
- limite D+7: `2026-09-23`;
- limite D+14: `2026-09-30`;
- fonte: D1 production / tabela `daily_metrics`;
- primeira data parcial: sim, 2026-09-16.

## Consulta de totais por evento

```sql
SELECT event_type, SUM(total) AS total
FROM daily_metrics
WHERE metric_date >= '2026-09-16'
  AND metric_date < 'LIMITE'
GROUP BY event_type
ORDER BY event_type;
```

## Totais principais

| Evento | Total |
| --- | ---: |
| `page_view` | — |
| `cta_click` | — |
| `form_start` | — |
| `lead_submit` | — |
| `guide_access` | — |
| `trail_start` | — |
| `trail_lesson_click` | — |
| `trail_complete` | — |

## Razões derivadas

- CTA CTR = `cta_click / page_view`: —
- conversão em lead = `lead_submit / page_view`: —
- lead após início de formulário = `lead_submit / form_start`: —
- conclusão de trilha = `trail_complete / trail_start`: —

Se o denominador for zero, registrar `N/A`, nunca `0%`.

## Segmentação complementar

Repetir a mesma janela, agregando por `path` e depois por `campaign`, apenas quando necessário para localizar o gargalo.

## Leitura de suficiência

Antes de escolher hipótese para a v1.8.0, registrar:

- volume absoluto dos denominadores;
- distribuição por `path`;
- concentração por campanha;
- presença de rotas históricas/legadas;
- efeito da primeira data parcial;
- qualquer mudança operacional que possa afetar a leitura.

Classificação do checkpoint:

- `INSUFICIENTE`: volume ou distribuição não sustentam hipótese;
- `EXPLORATÓRIO`: existe sinal, mas D+14 ainda é necessário para confirmação;
- `ACIONÁVEL`: há volume e padrão suficientes para formular uma hipótese única.

## Gargalo observado

- etapa do funil: —
- evidência quantitativa: —
- paths/campaigns relevantes: —
- limitações: —

Não preencher “gargalo” apenas com a menor taxa; considerar volume, contexto e qualidade da instrumentação.

## Decisão para a v1.8.0

- formular hipótese agora? `sim` / `não`;
- hipótese, se aplicável: —
- variável única candidata: —
- métrica primária: —
- métricas de guarda: —
- motivo para aguardar, se aplicável: —

A decisão deve seguir `v1.8.0-analysis-plan.md` e preservar a taxonomia existente.

## Evidência de integridade

- consulta executada foi somente leitura: —
- `rows_written = 0` / equivalente confirmado: —
- nenhum dado pessoal copiado para documentação pública: —
- nenhuma mudança de produção executada durante a extração: —

## Resultado do checkpoint

Resumo curto, factual e sem atribuir causalidade onde os dados apenas mostram correlação:

—
